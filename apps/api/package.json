{
  "name": "@vevit-games/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@vevit-games/engine": "workspace:*",
    "@vevit-games/rules": "workspace:*",
    "fastify": "^5.2.0"
  }
}
