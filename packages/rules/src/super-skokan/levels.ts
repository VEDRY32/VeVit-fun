/**
 * Úrovně Super skokana.
 *
 * Vlastní rozvržení, žádná kopie cizí hry. Všechny řádky mají stejnou délku.
 *
 * Legenda:
 *   `.` vzduch      `#` zem        `=` plošina (stoupá se na ni shora i zespoda)
 *   `o` mince       `^` trny       `@` start hráče
 *   `e` chodec (chodí sem a tam)   `s` skokan (poskakuje na místě)
 *   `F` cíl (vlajka)
 */

export interface LevelSpec {
  name: string;
  rows: string[];
}

export const LEVELS: LevelSpec[] = [
  {
    name: 'První kroky',
    rows: [
      '................................................',
      '................................................',
      '................................................',
      '..........ooo...................................',
      '.........=====.........ooo......................',
      '...............................=====............',
      '......o................o...............o........',
      '....=====...........=====...........=====.....F.',
      '@.........e.................e...................',
      '################################################',
    ],
  },
  {
    name: 'Nad propastí',
    rows: [
      '................................................',
      '...............ooo..............................',
      '..............=====.............ooo.............',
      '...........................o...=====............',
      '....ooo................=====....................',
      '...=====....s..................................F',
      '..........======..........=====.......======....',
      '@....e..........................................',
      '#######......####....####.....####....##########',
      '................................................',
    ],
  },
  {
    name: 'Trnitá cesta',
    rows: [
      '................................................',
      '.........ooo....................................',
      '........=====.........ooo.......................',
      '.....................=====......ooo.............',
      '..o.........s..................=====............',
      '.====...................e.............s........F',
      '............====...................=======......',
      '@.......^^......^^.......^^.....................',
      '####################....########################',
      '................................................',
    ],
  },
  {
    name: 'Schodiště',
    rows: [
      '................................................',
      '.........................................ooo...F',
      '........................................========',
      '..................................o.............',
      '.............................=========..........',
      '....................o...........................',
      '...............=========....e...................',
      '@....o....e.....................................',
      '####=========###################################',
      '................................................',
    ],
  },
];

/** Kontrola tvaru úrovně; prázdný seznam znamená, že je v pořádku. */
export function validateLevel(level: LevelSpec): string[] {
  const problems: string[] = [];
  const width = level.rows[0]?.length ?? 0;
  level.rows.forEach((row, i) => {
    if (row.length !== width) problems.push(`Řádek ${i} má ${row.length} znaků místo ${width}.`);
    for (const char of row) {
      if (!'.#=o^@esF'.includes(char)) problems.push(`Neznámý znak „${char}" na řádku ${i}.`);
    }
  });
  const count = (char: string): number =>
    level.rows.reduce((sum, row) => sum + [...row].filter((c) => c === char).length, 0);
  if (count('@') !== 1) problems.push(`Startů je ${count('@')}, musí být právě jeden.`);
  if (count('F') !== 1) problems.push(`Cílů je ${count('F')}, musí být právě jeden.`);
  if (count('o') === 0) problems.push('Úroveň nemá žádné mince.');
  return problems;
}
